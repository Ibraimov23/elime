const dishKey = "dish";
const drinkKey = "drink";
const numberKey = "number";
const removeKey = "remove"
const numberCookie = JSON.parse($.cookie(numberKey));
let dishStorage = JSON.parse(localStorage.getItem(dishKey));
let drinkStorage = JSON.parse(localStorage.getItem(drinkKey));
let count = 0;
let price = 0;
